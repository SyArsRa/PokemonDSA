#include <iostream>  
using namespace std;

template <typename type>
class Node{
    public:
        type* payload = NULL;
        int key = NULL;
};

template <typename type>
class MinMaxHeap{
    public:
        type* root = NULL;
        
};

